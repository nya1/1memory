/** Sentence embedding size for bundled `paraphrase-MiniLM-L3-v2` (Transformers.js / ONNX). */
export const EMBEDDING_MODEL_DIM = 384;

/** Directory name under `models/` (local ONNX + tokenizer). */
export const EMBEDDING_MODEL_DIR = "paraphrase-MiniLM-L3-v2";

export const EMBEDDING_ONNX_REL = "onnx/model_quantized.onnx";
